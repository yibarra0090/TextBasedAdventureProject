package Items;
import Person.Person;
import Items.Key;

    public class GhostKey implements Key {
        @Override
        public String getKey(Person x) {
            for (int i = 0; i < x.keys.length; i++){

            }
        }
        public String getName(){
            return "Ghost Key";
        }
    }

